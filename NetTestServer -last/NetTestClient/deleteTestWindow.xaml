<Window x:Class="NetTestClient.deleteTestWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:NetTestClient"
        mc:Ignorable="d"
        Title="试题记录删除" Height="450" Width="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="30"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <StackPanel Orientation="Horizontal" Grid.Row="0" >
            <TextBlock HorizontalAlignment="Left" TextWrapping="Wrap" Text="用户" VerticalAlignment="Top"/>
            <TextBox x:Name="txtUser" Text="Admin" HorizontalAlignment="Left" VerticalAlignment="Top" Width="74"/>
            <TextBlock HorizontalAlignment="Left" Text="密码" VerticalAlignment="Top" />
            <PasswordBox x:Name="txtPass" Password="123" HorizontalAlignment="Left" VerticalAlignment="Top" Width="74" />
            <Button x:Name="btGetTest" Content="获取试题" HorizontalAlignment="Left" VerticalAlignment="Top" Width="75" Click="btGetTest_Click"/>
            <Button x:Name="btDeleteTest" Content="删除试题" HorizontalAlignment="Left" VerticalAlignment="Top" Width="75" Click="btDeleteTest_Click"/>
        </StackPanel>
        <Grid Grid.Row="1" >
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="1*"/>
                <ColumnDefinition Width="1*"/>
            </Grid.ColumnDefinitions>
            <DataGrid x:Name="uGrid" Grid.Column="0" AutoGenerateColumns="False" IsReadOnly="True" SelectionMode="Extended"  SelectionChanged="uGrid_SelectionChanged"  CanUserDeleteRows="False" CanUserAddRows="False" CanUserReorderColumns="False" CanUserResizeRows="False">
                <DataGrid.Columns>
                    <DataGridTextColumn Binding="{Binding ID}" Header="ID" Width="50"/>
                    <DataGridTextColumn Binding="{Binding tDate}" Header="日期" Width="100"/>
                    <DataGridTextColumn Binding="{Binding tTitle}" Header="标题" Width="100"/>
                    <DataGridTextColumn Binding="{Binding tAnswer}" Header="答案" Width="50"/>
                </DataGrid.Columns>
            </DataGrid>
            <GridSplitter Width="3"/>
            <TextBox x:Name="txtText" Grid.Column="1" Text="{Binding tText,Mode=TwoWay}" VerticalScrollBarVisibility="Auto" />
        </Grid>
    </Grid>
</Window>
