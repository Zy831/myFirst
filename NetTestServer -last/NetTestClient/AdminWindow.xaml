<Window x:Class="NetTestClient.AdminWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:NetTestClient"
        xmlns:NetTest="clr-namespace:NetTestClient"
        mc:Ignorable="d"
        Loaded="Window_Loaded"
        Closing="Window_Closing"
        Title="AdminWindow" Height="450" Width="800">
    <Window.Resources>
        <NetTest:AnswerClass x:Key="answerList" />
    </Window.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="30"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <StackPanel Orientation="Horizontal" Grid.Row="0" >
            <Button x:Name="btDeleteTest" Content="删除试题" HorizontalAlignment="Left" VerticalAlignment="Top" Width="75" Click="btDeleteTest_Click" Margin="100,5,0,0"/>
            <Button x:Name="btAddTest" Content="添加试题" Width="75" Margin="50,5,0,0" HorizontalAlignment="Left" VerticalAlignment="Top" Click="btAddTest_Click"/>
            <Button x:Name="btUpdateTest" Content="更新试题" HorizontalAlignment="Left" VerticalAlignment="Top" Width="75" Click="btUpdateTest_Click" Margin="80,5,0,0"/>
        </StackPanel>
        <Grid Grid.Row="1" >
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="1*"/>
                <ColumnDefinition Width="1*"/>
            </Grid.ColumnDefinitions>
            <DataGrid x:Name="uGrid" Grid.Column="0" AutoGenerateColumns="False" SelectionMode="Extended" SelectionChanged="uGrid_SelectionChanged" CanUserDeleteRows="False" CanUserAddRows="False" CanUserReorderColumns="False" CanUserResizeRows="False">
                <DataGrid.Columns>
                    <DataGridTextColumn Binding="{Binding ID}" Header="ID" Width="50"/>
                    <DataGridTextColumn Binding="{Binding tDate}" Header="日期" Width="100"/>
                    <DataGridTextColumn Binding="{Binding tTitle}" Header="标题" Width="100"/>
                    <!--<DataGridTextColumn Binding="{Binding tAnswer}" Header="答案" Width="50"/>-->
                    <DataGridComboBoxColumn Header="答案" ItemsSource="{StaticResource answerList}" Width="50" SelectedValueBinding="{Binding tAnswer,Mode=TwoWay}" />
                </DataGrid.Columns>
            </DataGrid>
            <GridSplitter Width="3"/>
            <TextBox x:Name="txtText" Grid.Column="1" Text="{Binding tText,Mode=TwoWay}" VerticalScrollBarVisibility="Auto" />
        </Grid>
    </Grid>
</Window>
