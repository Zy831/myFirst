<Window x:Class="NetTestClient.UserTestWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:NetTestClient"
        xmlns:NetTest="clr-namespace:NetTestClient"
        mc:Ignorable="d"
        Loaded="Window_Loaded"
        Closing="Window_Closing"
        Title="UserTestWindow" Height="450" Width="800">
    <Window.Resources>
        <NetTest:AnswerClass x:Key="answerList" />
    </Window.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="30"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <StackPanel Orientation="Horizontal" Grid.Row="0" >
            <TextBlock HorizontalAlignment="Left" TextWrapping="Wrap" Text="用户" VerticalAlignment="Top"/>
            <TextBox x:Name="txtUser" Text="Admin" HorizontalAlignment="Left" VerticalAlignment="Top" Width="74"/>
            <TextBlock HorizontalAlignment="Left" Text="密码" VerticalAlignment="Top" />
            <PasswordBox x:Name="txtPass" Password="123" HorizontalAlignment="Left" VerticalAlignment="Top" Width="74" />
            <Button x:Name="btGetTest" Content="获取试题" HorizontalAlignment="Left" Margin="100,5,100,0" VerticalAlignment="Top" Width="75" Click="btGetTest_Click"/>
            <Button x:Name="btHandleTest" Content="提交答案" HorizontalAlignment="Center" VerticalAlignment="Top" Width="75" Margin="0,5,0,0" Click="btHandleTest_Click"/>
            <TextBlock x:Name="msg" Text="" Foreground="Red" />
        </StackPanel>
        <Grid Grid.Row="1" >
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="120"/>
                <ColumnDefinition Width="1*"/>
            </Grid.ColumnDefinitions>
            <DataGrid x:Name="uGrid" Grid.Column="0" AutoGenerateColumns="False" SelectionMode="Single"  SelectionChanged="uGrid_SelectionChanged"  CanUserDeleteRows="False" CanUserAddRows="False" CanUserReorderColumns="False" CanUserResizeRows="False">
                <DataGrid.Columns>
                    <DataGridTextColumn Binding="{Binding tNo}" Header="序号" Width="40" IsReadOnly="True"/>
                    <DataGridComboBoxColumn Header="答案" ItemsSource="{StaticResource answerList}" Width="50" SelectedValueBinding="{Binding uAnswer,Mode=TwoWay}" />
                </DataGrid.Columns>
            </DataGrid>
            <GridSplitter Width="3"/>
            <TextBox x:Name="txtText" Grid.Column="1" Text="{Binding tText,Mode=TwoWay}" VerticalScrollBarVisibility="Auto" />
        </Grid>
    </Grid>
</Window>
